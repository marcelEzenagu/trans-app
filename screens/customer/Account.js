import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Account = ({navigation}) => {
    return (
        <View>
            <Text>Im account</Text>
        </View>
    )
}

export default Account

const styles = StyleSheet.create({})
